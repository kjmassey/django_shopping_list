# This is a template for DRF projects
### - Uses a 'config' folder to avoid nested folders with the same name
### - sqllite3
### - Initial migrations completed, no models, views or serializers
### - No other apps, extensions or middleware